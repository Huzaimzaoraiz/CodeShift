\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc}

\begin{document}
\begin{tikzpicture}[
    auto,
    node distance=0.8cm and 0cm,
    base/.style={draw, align=center, minimum height=0.8cm, minimum width=6cm, font=\sffamily\small, rounded corners},
    llm/.style={base, fill=purple!10, draw=purple, thick},
    data/.style={base, fill=blue!10, draw=blue!70, thick},
    logic/.style={base, fill=green!10, draw=green!60!black, thick},
    context/.style={base, fill=orange!20, draw=orange, thick},
    arrow/.style={-{Stealth[length=2.5mm, width=2.5mm]}, thick, draw=black!70}
]

% =====================
% Nodes (Vertical Flow)
% =====================

\node[context] (session) {User Session};

\node[context, below=of session] (rbac) {Persona + Data Entitlements / RBAC};

\node[data, below=of rbac] (dbs) {Company Database / Data Sources};

\node[logic, below=of dbs] (dbconn) {DB Connector Layer (read-only)};

\node[logic, below=of dbconn] (schemaintro) {Schema Introspector};

\node[llm, below=of schemaintro] (schemamap) {Schema Mapper (LLM)};

\node[data, below=of schemamap] (enhancedkpi) {Enhanced KPI};

\node[data, below=of enhancedkpi] (kpikb) {KPI Knowledge Base};

\node[logic, below=of kpikb] (kpicheck) {KPI Computability Check};

\node[logic, below=of kpicheck] (mlengine) {Causal ML / Time-Series ML / Statistical Rules};

\node[data, below=of mlengine] (evgraph) {Evidence Graph};

\node[llm, below=of evgraph] (reasoner) {Reasoning Model};

\node[logic, below=of reasoner] (confscorer) {Confidence Scorer};

\node[llm, below=of confscorer] (storygen) {Story Generator};

\node[context, below=of storygen] (telemetry) {Telemetry};

% =====================
% Edges
% =====================

\draw[arrow] (session) -- (rbac);
\draw[arrow] (rbac) -- (dbs);
\draw[arrow] (dbs) -- (dbconn);
\draw[arrow] (dbconn) -- (schemaintro);
\draw[arrow] (schemaintro) -- (schemamap);
\draw[arrow] (schemamap) -- (enhancedkpi);
\draw[arrow] (enhancedkpi) -- (kpikb);
\draw[arrow] (kpikb) -- (kpicheck);
\draw[arrow] (kpicheck) -- (mlengine);
\draw[arrow] (mlengine) -- (evgraph);
\draw[arrow] (evgraph) -- (reasoner);
\draw[arrow] (reasoner) -- (confscorer);
\draw[arrow] (confscorer) -- (storygen);
\draw[arrow] (storygen) -- (telemetry);

\end{tikzpicture}
\end{document}
